import { Input, InputProps } from 'antd'import type { ChangeEvent } from 'react'import { omit } from 'lodash'interface IModelInputProps extends InputProps {    model: string    setModel: (value: string) => void}const ModelInput = (props: IModelInputProps) => {    const handleInputChange = (e: ChangeEvent) => {        const target = e.target as HTMLInputElement        props.setModel(target.value)    }    const otherProps = omit(props, ['model', 'setModel'])    return <Input value={ props.model } onChange={ handleInputChange } { ...otherProps }></Input>}export default ModelInput