import { Config } from '@/pages/ProcessDesign/core/Process/Config.ts'import { Process } from '@/pages/ProcessDesign/core/Process'import { GraphConfigData } from '@logicflow/core/types/type'interface IApplicationProps {    name?: string}export class Application {    public name!: string    public process!: Process    public config!: Config    constructor (props: IApplicationProps = {}) {        this.name = props.name || '新流程'        this.config = new Config(this)    }    public initProcess(process: Process): void {        this.process = process    }    public getGraphData(): GraphConfigData {        return this.process.getGraphData()    }    public updateConfig(data: IObj): void {        this.config.update(data)    }}