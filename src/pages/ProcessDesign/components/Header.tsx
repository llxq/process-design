import ModelInput from '@/components/ModelInput'import ConfigModal from '@/pages/ProcessDesign/components/ConfigModal.tsx'import type { Application } from '@/pages/ProcessDesign/core/Application.ts'import { useState } from 'react'import { Button } from 'antd'import '../assets/styles/header.scss'import { createPortal } from 'react-dom'const Header = ({ app }: { app: Application }) => {    const [ name, setName ] = useState(app.name)    const [ showConfigModal, setShowConfigModal ] = useState<boolean>(false)    const save = () => {        localStorage.setItem('data', JSON.stringify(app.getGraphData()))    }    const updateConfig = () => {        setShowConfigModal(true)    }    return (        <div className="process-design__header flex-between">            <div className="left">                <ModelInput model={ name }                            setModel={ setName }                            variant="borderless"                            placeholder="请输入流程名称"                            maxLength={ 50 } />            </div>            <div className="right">                <Button type="primary" onClick={ updateConfig }>修改配置</Button>                <Button type="primary" onClick={ save }>保存</Button>            </div>            {                showConfigModal && createPortal(<ConfigModal onSetShowConfigModal={ setShowConfigModal } app={ app } />, document.body)            }        </div>    )}export default Header